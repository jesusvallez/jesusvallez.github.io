---
import Layout from '@/layouts/Layout.astro'
import Experience from '@/components/Experience.astro'
import SocialMedia from '@/components/SocialMedia.astro'

import { info } from '../data/info'

const { name, experience, aboutMeShort } = info
const jobDescription = experience['Experiencia Profesional'][0].job[0].name
const jobCompany = experience['Experiencia Profesional'][0].name
---

<Layout>
  <div class="grid w-full max-w-6xl grid-cols-1 grid-rows-1 gap-x-4 gap-y-4 lg:grid-cols-[1fr_1fr]">
    <aside class="mt-6 flex flex-col gap-4 px-6 md:mt-20 lg:px-4">
      <img
        class:list={['h-32 w-32 rounded-full object-cover', 'md:h-40 md:w-40', 'lg:h-52 lg:w-52']}
        src="/ogimage.png"
        alt="Imagen de Jesús Vállez"
        width="740"
        height="420"
      />

      <p class="text-body1">
        ¡Hey! <span data-testid="emoji" role="img" aria-label="Mano saludando">👋🏻</span>, mi nombre es
      </p>

      <h1
        class:list={[
          'mx-0 inline font-bold leading-tight text-transparent [font-size:_clamp(28px,_5vw+12px,_50px)] sm:block md:[font-size:_clamp(28px,_4vw+4px,_50px)]',
          'animate-[gradient_5s_ease_infinite] bg-gradient-to-r from-my-secondary via-my-primary to-my-tertiary bg-[length:150%_100%] bg-clip-text',
        ]}
      >
        {name}
      </h1>
      <h2
        class:list={[
          'mx-0 [text-wrap:pretty]',
          'inline text-h6 font-bold leading-tight sm:block',
          'md:text-h5 md:leading-tight',
        ]}
      >
        Actualmente trabajo como {jobDescription} en {jobCompany}
      </h2>
      <p class="max-w-3xl text-body1 [text-wrap:pretty]">
        {aboutMeShort}
      </p>
      <SocialMedia items={info.socialMedia} />
    </aside>
    <section>
      {Object.entries(info.experience).map(([key, value]) => <Experience name={key} job={value} />)}
    </section>
  </div>
</Layout>
